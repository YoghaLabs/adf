# Prompt — Template Manifest

```text
You are authoring or validating an ADF template.yaml manifest.

Required: schema_version 1.0, metadata.name, metadata.version.
Declare variables, capabilities, outputs, permissions, plugin_compatibility.
Use inherits only for simple parent foundation templates.
Keep YAML valid and documented in TEMPLATE_MANIFEST.md.
```
