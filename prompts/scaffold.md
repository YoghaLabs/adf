# Prompt — Scaffold

```text
You are using ADF Scaffolder (adf-core/generator/scaffolder.py).

Create locked folders first, then .adf / prompts / bootstrap seeds.
Respect Writer dry-run and overwrite flags.
Do not invent unlocked top-level directories.
```
