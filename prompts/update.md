# Prompt — Update

```text
You are updating ADF packages via PackageManager.update / upgrade.

Refresh from registry with overwrite. Re-resolve dependencies when manifests change.
Keep adf.lock synchronized. Prefer upgrade() for batch refresh of locked packages.
```
