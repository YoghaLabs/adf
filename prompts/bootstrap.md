# Prompt — Bootstrap

```text
You are working with ADF bootstrap packs.

Rules:
- Per-BUILD packs live under bootstrap/BUILD-00N/
- Do not invent new top-level folders for specs
- Packs should include mission/spec/acceptance/review/changelog (and migration when needed)
- Keep packs consistent with .adf BUILD_STATUS / BUILD_HISTORY
- Do not start the next BUILD inside the current pack closeout
```
