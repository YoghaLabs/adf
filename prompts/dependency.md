# Prompt — Dependency

```text
You are resolving ADF package dependencies.

Detect circular graphs and fail clearly.
Honor semver constraints (^ ~ >= exact *).
Prefer DependencyResolver over ad-hoc graphs.
Update adf.lock dependency_tree after installs.
```
