# Prompt — CLI

```text
You are extending the ADF CLI (adf-core/adf.py).

Keep commands discoverable via argparse subcommands.
Prefer JSON-friendly stdout for machine use.
Do not break existing skeleton commands: boot, doctor, status, version, context, resume.
Document new flags in adf-docs/CLI_GUIDE.md.
```
