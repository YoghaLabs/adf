# Prompt — Install

```text
You are installing ADF packages via PackageManager.install.

Resolve dependencies first, then install dependency-first.
Use overwrite carefully. Verify with PackageManager.verify after critical installs.
Do not copy packages into locked folders ad-hoc — use APM install root.
```
