# Prompt — Service

```text
You are working on the ADF Service Layer (adf-core/services/).

Services orchestrate existing engines. Never duplicate business logic.
CLI/SDK/Studio/Plugins must call services via ServiceManager.
Use BaseService + ServiceResult envelopes.
Do not start BUILD-011 from a service task.
```
