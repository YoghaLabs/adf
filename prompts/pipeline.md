# Prompt — Pipeline

## Components

PipelineManager · stages · execution · history · metrics

## Rules

- `PipelineExecution.autonomous` is always `false` in BUILD-018
- Transitions are displayed, not auto-applied from Studio as SSOT
- Stages: Planning → … → Release (+ Custom)
