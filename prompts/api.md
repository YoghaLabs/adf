# Prompt — Public API

```text
You are maintaining the ADF public Python API (package adf).

Supported:
  from adf import RuntimeService, PackageService, GeneratorService, SDKClient

CLI entry is adf.cli:main. Do not reintroduce direct engine imports in adf.cli.
Documentation SSOT: adf-docs/PUBLIC_API.md and ADR-008.
```
