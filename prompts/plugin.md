# Prompt — Plugin

```text
You are working on ADF plugins.

Use contracts.BasePlugin / AbstractPlugin.
Register via PluginManager — RuntimeEngine must not new up concrete plugins.
Add tests for register/load/validate.
Update docs when adding plugin types.
```
