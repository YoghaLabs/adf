# Prompt — RBAC

## Hierarchy

System → Organization → Workspace → Project → Custom

## Rules

- Roles carry permission ids; matrix shows grants / inheritance / overrides
- Policy resolution order is hierarchical
- Presentation helpers only — Service Layer owns enforcement
