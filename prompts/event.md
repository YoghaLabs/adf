# Prompt — Event

```text
You are working on the ADF EventBus.

Keep lifecycle event names stable (on_boot, on_resume, on_load, on_save, on_commit, on_handoff).
Handlers must be synchronous and side-effect explicit.
Add/adjust pytest for dispatch + history.
```
