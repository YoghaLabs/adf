# Prompt — Verification

```text
You are verifying ADF packages.

Combine PackageManager.verify (lockfile/install) with PackageSecurity.verify_package.
CLI: adf verify [package_id] via RegistryService only.
```
