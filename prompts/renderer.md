# Prompt — Template Renderer

```text
You are rendering ADF templates via TemplateRenderer / TemplateManager.render.

Respect overwrite=False by default.
Render UTF-8 text with variable substitution; copy binary files unchanged.
Expand variables in relative path segments when present.
Prefer TemplateManager over calling renderer internals from CLI/plugins.
```
