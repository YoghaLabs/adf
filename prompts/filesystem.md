# Prompt — Filesystem

```text
You are using ADF generator FileSystem/Writer helpers.

Default overwrite=False. Dry-run must not create real directories/files.
Fail clearly when destination is non-empty without overwrite.
Record progress through GenerationOutput.
```
