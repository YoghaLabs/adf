# Prompt — Marketplace

```text
You are working on the ADF Marketplace (presentation over Registry).

MarketplaceManager must not re-implement PackageManager install logic.
Browse/search/favorites/collections only; install/update/publish via RegistryManager.
Studio uses marketplace.studio_api() / MarketplaceService / SDK MarketplaceClient.
```
