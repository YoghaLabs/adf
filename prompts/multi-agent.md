# Prompt — Multi-Agent Model

## Graph (architecture only)

Planner → Architect → Backend & Frontend → QA → Documentation → Release

## Rules

- Model data + UI + SDK only
- **Do not** automate handoffs in BUILD-017 / until explicitly unlocked
- Preserve participant identity for each node
