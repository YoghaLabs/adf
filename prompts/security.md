# Prompt — Security

```text
You are hardening ADF package security checks.

PackageSecurity: checksum (when declared), opaque signature field, trusted publishers,
capabilities-as-permissions. Full PKI is deferred; do not fake networked crypto.
```
