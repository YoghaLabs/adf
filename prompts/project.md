# Prompt — Project

```text
You may be defining an ADF ProjectManifest for generation OR using ProjectService.

Generation: Required name. Defaults template=generic (or foundation when specified),
author=YoghaLabs, version=0.1.0-alpha. Validate before generate when unsure.

ProjectService / SDK projects(): expose identity + state for Studio via services only.
Do not read engine internals from CLI/Studio code paths.
```
