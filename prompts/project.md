# Prompt — Project

```text
You are defining an ADF ProjectManifest for generation.

Required: name. Defaults: template=foundation, author=YoghaLabs, version=0.1.0-alpha.
Pass extras only for declared template variables.
Validate before generate when unsure.
```
