# Prompt — Variables

```text
You are using the ADF variable system (VariableResolver).

Syntax is {{name}} and {{nested.key}} only.
Prefer strict mode. Supply overrides at render time.
Merge order: parent defaults → child defaults → caller overrides.
```
