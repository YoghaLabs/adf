# Prompt — Permissions

## Model

Fine-grained permissions · matrix · inheritance · overrides · policy resolution

## Rules

- SDK: `PermissionClient`
- Do not invent permission checks inside React as SSOT
