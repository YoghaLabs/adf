# BUILD-016 Migration

## From BUILD-015

Visual Intelligence remains. `/runtime` now hosts the full AI Runtime Dashboard
(replacing the BUILD-013 status skeleton).

## Operator notes

1. `cd adf-studio && npm install && npm test`
2. Open Sidebar → Runtime
3. Fixtures until Core observability APIs land

## Breaking changes

None for Python CLI/SDK. Studio `/runtime` page UX expands significantly.
