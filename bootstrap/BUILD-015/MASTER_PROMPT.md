# BUILD-015 Master Prompt (archived)

Implement **Visual Intelligence Platform** for ADF Studio.

Read-only graphs via React Flow. UI → SDK → Service Layer → Core.

Deliver graph engine, all graph modules, dashboards, search, stores, SDK
clients, tests, docs, ADR-013, bootstrap, version `0.15.0-alpha`.

**STOP** before BUILD-016.
