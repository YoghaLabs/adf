# BUILD-015 Migration

## From BUILD-014

Workspace Experience remains. Visual Intelligence adds `/visual` and graph
routes without changing workspace→project→session hierarchy.

## Operator notes

1. `cd adf-studio && npm install && npm test`
2. Open Sidebar → Visual
3. Graphs are fixtures until Core graph APIs land

## Breaking changes

None for Python CLI/SDK. Studio gains `@xyflow/react` dependency.
