# BUILD-013 Master Prompt (archived)

Implement **ADF Studio Core** — Desktop Control Center (not an IDE).

Stack: Tauri (preferred) + React + TypeScript + Tailwind + Zustand + TanStack Query.

Control flow: `UI → SDK → Service Layer → ADF Core`. No business logic in Studio.

Deliver application shell, navigation, dashboard widgets, pages, stores, SDK
adapters, themes, tests, docs, ADR-011, bootstrap pack, version `0.13.0-alpha`.

**STOP** before BUILD-014.
