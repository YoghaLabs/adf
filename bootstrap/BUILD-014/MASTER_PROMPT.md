# BUILD-014 Master Prompt (archived)

Implement **Workspace Experience Platform** for ADF Studio.

Hierarchy: Workspace → Projects → Sessions.

UI → SDK → Service Layer → Core. No business logic in Studio.

Deliver workspace manager, project explorer, session manager, activity feed,
search platform, stores, SDK clients, tests, docs, ADR-012, bootstrap,
version `0.14.0-alpha`.

**STOP** before BUILD-015.
