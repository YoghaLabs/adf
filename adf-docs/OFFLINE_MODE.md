# Offline Mode

OfflineDistributor creates:

- Offline registry snapshot (via RegistrySync)
- Offline installer descriptor
- Offline bundles (registry + docs + bootstrap note)

CLI: `adf bundle <source> --kind offline --version …`
