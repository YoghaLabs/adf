# Knowledge Graph

**SSOT for Studio Knowledge Graph view.** BUILD-015.

## Surface

`/visual/knowledge` — nodes for knowledge, context, workspace; edges for
contains / references / uses / extends.

## Data

`KnowledgeClient.graph()` / `GraphClient.get("knowledge")` — fixture until Core
knowledge services expose graph envelopes.

## Related

- `VISUAL_PLATFORM.md`
- ADR-013
