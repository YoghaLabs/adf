# Sessions

- Better Auth session cookies for Studio UI
- Device list + login history tables in ADF identity DB
- Session revocation / refresh handled by Better Auth APIs
- PATs / API tokens are separate hashed credentials (`personal_access_tokens`)
