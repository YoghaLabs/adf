# Workspace (Identity)

Identity workspaces belong to an Organization and are distinct from ADF Core
filesystem workspaces (`workspace.describe`).

- `workspaces.organization_id` required
- Projects inherit organization permissions
- Studio selector pages: `/identity/workspaces`
