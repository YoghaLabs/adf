# Scaffolder

**Class:** `Scaffolder`  
**Module:** `adf-core/generator/scaffolder.py`

Creates the locked top-level folder set and baseline documentation for a new
ADF project before template rendering. Uses `Writer` + `FileSystem` so dry-run
and overwrite rules stay consistent.
