# Windows Installer

- Format: `.msi` / `.exe` (Tauri / WiX as available)
- Installs Studio + optional CLI shim
- Per-user default; machine-wide optional for Enterprise Bundle
- Authenticode signing required for GA
