# APM Local Registry

Local package registry for the ADF Package Manager (BUILD-009).

Each subdirectory is a package containing ``package.yaml``.

Remote backends (GitHub / GitLab / private) are prepared in code but disabled
by default — see ADR-007.
