# Memory

- Operator standing rule: finish → commit → push develop → merge main → VPS pull
- Studio is control center, not IDE (Product Positioning)
- FO = live Core in Studio + honest demo labeling + GA gates; not extensions/cloud
- BUILD-021 L1: hybrid bridge via `python -m adf.studio_bridge` and `/adf-bridge/invoke`
- VPS Studio: Tailscale `http://100.64.209.99:1420/`, systemd `adf-studio.service`
- Set `ADF_PYTHON` to venv python if live bridge fails on VPS
